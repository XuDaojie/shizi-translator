{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Tauri Dev (npm run tauri dev)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run tauri dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
